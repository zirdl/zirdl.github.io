#The base URL of the site; the only required configuration variable.
base_url = "https://zirdl.github.io"
theme = "kita"
# The site title and description; used in feeds by default.
title = "zirdl⌨"
description = "Welcome to my website"

# The default site author.
author = "gio"

# The default language; used in feeds.
default_language = "en"

# When set to "true", a feed is automatically generated.
generate_feed = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a built-in template that renders an Atom 1.0 feed.
# There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filename = "atom.xml"

# The taxonomies to be rendered for the site and their configuration of the default languages
# Example:
#     taxonomies = [
#       {name = "tags", feed = true}, # each tag will have its own feed
#       {name = "tags"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [{ name = "tags", rss = true, paginate_by = 5 }]

# Configuration of the Markdown rendering
[markdown]
# When set to "true", all code blocks are highlighted.
highlight_code = true

# A list of directories used to search for additional `.sublime-syntax` and `.tmTheme` files.
extra_syntaxes_and_themes = []

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "base16-ocean-dark"

[extra]
math = false
mermaid = false
comment = true

[extra.style]
# The custom background color.
# bg_color = ""

# The custom background color in dark mode.
# bg_dark_color = ""

# Enable header blur.
header_blur = true

# The custom header color.
header_color = ""

# The custom header color in dark mode.
# header_dark_color = ""
[extra.profile]
name = "zirdl"
bio = "Just another INTP with a boring website. More on me on the About page:}"
avatar_url = "/imgs/dp.png"
avatar_invert = false 

[[extra.profile.social]]
name = "github"
url = "https://github.com/zirdl"

[[extra.profile.social]]
name = "rss"
url = "$BASE_URL/atom.xml"

# [[extra.menu]]
# name = "Projects"
# url = "$BASE_URL/projects"


# [[extra.menu]]
# name = "Archives"
# url = "$BASE_URL/archive"

[[extra.menu]]
name = "🖼"
url = "$BASE_URL/giography"

[[extra.menu]]
name = "Tags"
url = "$BASE_URL/tags"

[[extra.menu]]
name = "About"
url = "$BASE_URL/about"


# F O O T E R 

[extra.footer]
# since = 2024
license = "CC BY-SA 4.0"
license_url = "https://creativecommons.org/licenses/by-sa/4.0/deed"

# G I S C U S   C O M M E N T S
[extra.giscus]
repo = "zirdl/zirdl.github.io"
repo_id = "R_kgDOKb41Ug"
category = "Ideas"
category_id = "DIC_kwDOKb41Us4CeCAm"
mapping = "title"
strict = 1
reactions_enabled = 1
emit_metadata = 0
input_position = "top"
theme = "transparent_dark"
lang = "en"
loading = "lazy"
